# Default values for fynd_chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
name: fynd-chart2
image:
  repository: jainankur2/project4
  pullPolicy: IfNotPresent
  tag: "25"

service:
  name: Node2
  type: LoadBalancer 
  targetPort: 30066
  servicePort: 80

